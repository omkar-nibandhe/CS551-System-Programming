#Targets:
#  Default target: build project
#  clean:          remove all generated files.
#  submit:         build compress archive all project source files.


CFLAGS = -g  -Wall -std=c11

all: main.c word-store.c word-store.h
	gcc main.c -o word-count
	
clean:		
		rm -f word-count



